extends layout_columns

block header
    include headers/generic

block inner_content
    div#content.col-8.col-md-12
        h1.text-center Benchmarks

        section#graphs
            h2 Graphs (generated by the CI)
            div.inner-section
                p
                    | Graphs on this page represent the relative difference in performance between ArkScript and other languages on
                    | specifics inputs (see the #[a(href="https://github.com/ArkScript-lang/benchmarks") ArkScript-lang/benchmarks] repository).
                p
                    | Values are computing the #[code mean] of multiple runs: #[code ArkScript.mean / Other.mean].
                    | A value of #[code 1] means ArkScript is as fast as the other language, higher means ArkScript is slower by this amount.
                p
                    | In a perfect world, the graphs should always go down, or at least not up. An upward trend can mean that a performance
                    | regression was introduced.
                canvas#linechart

append scripts
    script(src="https://cdn.jsdelivr.net/npm/chart.js@4.4.3/dist/chart.umd.min.js")
    script(src="/assets/js/moment.js")
    script(src="/assets/js/chartjs-adapter-moment.js")
    script(src="/assets/js/graph-benchmarks.js")

